{
  "name": "elysia-typecheck-bug-repro",
  "version": "1.0.50",
  "scripts": {
    "build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile build",
    "build-linux-arm": "bun build --compile --target=bun-linux-arm64 --minify --sourcemap ./src/index.ts --outfile build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/bearer": "^1.2.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
